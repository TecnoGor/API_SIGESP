// ? VERIFICADA - 27-07-2026
export interface IRetencionIslr {
    numcom: string;
    numsol: string;  
    numope: string;
    numfac: string;  
    numcon: string;  
    fecfac: string;  
    cmp_codret: string;  
    consol: string;  
    totcmp_con_iva: string;
    basimp: string;
    sustraendo: string;
    porded: string;  
    cmp_monret: string;
    rif: string;
    nomsujret: string;
    email: string;
    dirsujret: string;  
    telefono: string;  
    id_codigo_ret: string;
    num_control: string;
}